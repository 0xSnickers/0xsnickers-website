---
title: CSS
description: CSS 样式生态笔记。
---

这里记录前端样式、组件库和 UI 基础设施相关的笔记。

CSS 生态不只是写样式本身，还包括：

- 怎么快速搭建一套统一的 UI。
- 怎么保证弹窗、下拉框、菜单这类交互组件好用、可访问、可维护。
- 怎么让组件既能复用，又不把项目锁死在某个重型组件库里。

当前重点：

- `Tailwind CSS`：偏“原子化样式工具类”的 CSS 编写方案。
- `shadcn/ui`：偏“可复制、可改造”的组件代码方案。
- `@radix-ui`：偏“无样式、负责交互能力”的底层组件原语。
